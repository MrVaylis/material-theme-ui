plugins {
    id 'java'
}

group = 'me.naulbimix'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compileOnly fileTree(dir: 'libs', include: ['*.jar'])
    compileOnly fileTree(dir: 'X:/dev/JetBrains/IntelliJ IDEA 2025.2/lib', include: ['*/**.jar', '*.jar'])
}